anthropic==0.40.0
fastapi==0.115.0
uvicorn[standard]==0.30.6
websockets==13.1
python-dotenv==1.0.1
pydantic==2.8.2
pydantic-settings==2.5.2
sqlalchemy[asyncio]==2.0.36
aiosqlite==0.20.0          # mémoire Karl (knowledge_base SQLite)
httpx==0.27.2              # appels VPS agent + Cloudflare API + Telegram
jinja2==3.1.4
pyjwt==2.9.0
python-multipart==0.0.12   # upload fichiers via FastAPI

# ─── Providers LLM optionnels ──────────────────────────────
# Décommenter selon le PROVIDER choisi dans .env

# OpenAI (PROVIDER=openai) ou Ollama (PROVIDER=ollama)
# openai>=1.40.0

# Google Gemini (PROVIDER=gemini)
# google-generativeai>=0.8.0

# ─── Analytics GA4 (optionnel) ─────────────────────────────
# google-analytics-data==0.18.12
# google-auth==2.35.0
